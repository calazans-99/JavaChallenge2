/* V0__extensions.sql (SQL Server)
   NOOP: no SQL Server não há extensões tipo Postgres.
   Para UUID, use em cada tabela:
     coluna UNIQUEIDENTIFIER NOT NULL DEFAULT NEWID()
   ou   UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID()
*/
